import classes from "./results.module.css";
import React from "react";


const results = (props) =>{
    
    // let res = props.results.map((result, i )=>{
    //     <div>{res}</div>
    // })
    return (<div>{console.log(props.results)}</div>);
}

export default results;